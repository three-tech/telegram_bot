接收转的图文消息，设计表结构来保存图文消息，保存图片/视频的fileId在数据库中，以备后续转发使用

任务详情如下：
1. 设计表结构，表名为`telegram_bot_message`
2. 接收的报文消息为：[receivedMessageGroup.json](receivedMessageGroup.json)[receivedPhotoMessage.json](receivedPhotoMessage.json)[receivedVedioMessage.json](receivedVedioMessage.json)
3. 根据报文消息，重新设计表结构，使其至少满足以上三种报文消息
4. 查询telegramBot的官方文档，如果要根据file_id转发图片/视频，表中应该设计哪些必要字段
5. 使用本地数据库sqllite3


强制要求要求：
1. 使用python12的语法规则，使用conda环境telegram_bot环境
2. 严格遵循本需求文档的要求，禁止添加额外的功能
3. 严格遵循[AGENTS.md](../AGENTS.md)的要求
4. 先阐述行动计划，得到明确的批准后才可以动代码
5. 禁止返回模拟数据，禁止写mock数据，代码中不允许出现todo
6. 代码中所有英文注释，都必须翻译成中文，你也必须使用中文回复

